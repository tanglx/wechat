server:
  port: 11000

spring:
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    druid:
      url: jdbc:mysql://49.232.144.206:3306/wechat?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
      username: root
      password: 123123
      # ?????
      initialSize: 5
      # ???????
      minIdle: 10
      # ???????
      maxActive: 20
      # ?????????????
      maxWait: 60000
      # ???????????????????????????????
      timeBetweenEvictionRunsMillis: 60000
      # ??????????????????????
      minEvictableIdleTimeMillis: 300000
      # ??????????????????????
      maxEvictableIdleTimeMillis: 900000
      # ??????????
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      webStatFilter:
        enabled: true
      statViewServlet:
        enabled: true
        # ???????????????
        allow:
        url-pattern: /druid/*
        # ???????????
        login-username: admin
        login-password: 123123
      filter:
        stat:
          enabled: true
          # ?SQL??
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
  redis:
    database: 0
    host: 192.168.20.31
    password: 123123
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 1000ms
      shutdown-timeout: 100ms

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: mapper/*.xml
  global-config:
    db-config:
      logic-delete-value: 0 #??????(??? 1)
      logic-not-delete-value: 1 #??????(??? 0)
#logging:
#  level:
#    root: debug

wechat:
  appID: wxe323a3ff8027d3db
  secret: 85deb25a1ee5b6ed0081b1bfb9c1bb55
  token: #???????Token
  encodingAESKey: #???????EncodingAESKey
